openapi: 3.0.3
info:
  title: WACZ internal artifact formats
  version: 0.1.0
paths: {}
components:
  schemas:
    WaczIndexEntry:
      type: object
      additionalProperties: false
      required: [url, digest, mime, filename, length, offset, status, recordDigest]
      properties:
        url:
          type: string
        digest:
          type: string
        status:
          type: integer
          format: int64
        mime:
          type: string
        filename:
          type: string
        length:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        recordDigest:
          type: string
    WaczPagesHeader:
      type: object
      additionalProperties: false
      required: [format, id, title, hasText]
      properties:
        format:
          type: string
        id:
          type: string
        title:
          type: string
        hasText:
          type: boolean
    WaczResource:
      type: object
      additionalProperties: false
      required: [name, path, hash, bytes]
      properties:
        name:
          type: string
        path:
          type: string
        hash:
          type: string
        bytes:
          type: integer
          format: int64
    WaczDataPackage:
      type: object
      additionalProperties: false
      required: [profile, resources, wacz_version, title, software, created, modified]
      properties:
        profile:
          type: string
        resources:
          type: array
          items:
            $ref: "#/components/schemas/WaczResource"
        wacz_version:
          type: string
        title:
          type: string
        software:
          type: string
        created:
          type: string
          format: date-time
        modified:
          type: string
          format: date-time
