[tool.ruff]
line-length = 100
target-version = "py311"
[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
]

[tool.sqlfluff.core]
dialect = "postgres"
max_line_length = 100
exclude_rules = ["RF04"]
